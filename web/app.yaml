runtime: nodejs22

instance_class: F1

env_variables:
  # Replace <VM_IP> with your backend VM's external IP address before deploying
  DATABASE_URL: "postgresql://ent_admin:localdevpassword@<VM_IP>:5432/ent_surveillance"

handlers:
  - url: /.*
    script: auto
